@model IEnumerable<EIS.Models.EmpDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Welcome @Session["Fname"]</h2>

<table class="table">
    

<tr>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = Session["Emp_id"] }) |
        @Html.ActionLink("Details", "Details", new { id = Session["Emp_id"] }) |
    </td>
    </tr>


</table>
